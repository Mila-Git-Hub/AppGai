<Page x:Class="AppGai.pgwin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AppGai"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="pgwin">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Background="#b4c7e7">
            <StackPanel>
                <Image Source="C:\Program Files (x86)\Microsoft Visual Studio\2019\Community\Common7\IDE\Logo.png" Width="100" Margin="10"/>
                <Button Content="Нарушения" Click="ShowViolations" Margin="10"/>
                <Button Content="Водители" Click="ShowDrivers" Margin="10"/>
                <Button Content="Автомобили" Click="ShowCars" Margin="10"/>
            </StackPanel>
        </Grid>
        <Frame Grid.Column="1" x:Name="myFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
